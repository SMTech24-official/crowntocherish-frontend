import LogIn from '@/components/auth/Login';
import React from 'react';

const page = () => {
    return (
        <div>
            
            <LogIn />
        </div>
    );
};

export default page;