

export interface CardProps {
    title: string
    description: string
    background: string
    height: string
    titleColor: string
    textColor: string
    buttonColor: string
    buttonHoverColor: string
    icon?: React.ReactNode
  }